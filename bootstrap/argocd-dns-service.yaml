apiVersion: v1
kind: Service
metadata:
  name: argocd-dns
  namespace: argocd
  annotations:
    external-dns.alpha.kubernetes.io/hostname: argocd.fedishark.eu
spec:
  type: ExternalName
  externalName: jump.fedishark.eu
