apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: dirigera-exporter
  namespace: monitoring
  labels:
    release: kube-prometheus-stack
spec:
  selector:
    matchLabels:
      app.kubernetes.io/name: dirigera-exporter
      app.kubernetes.io/instance: dirigera-exporter
  namespaceSelector:
    matchNames:
      - monitoring
  endpoints:
    - port: http
      path: /metrics
      interval: 30s
      scrapeTimeout: 10s
