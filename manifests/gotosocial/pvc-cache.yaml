apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: gotosocial-cache
  namespace: gotosocial
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: truenas-ssd
  resources:
    requests:
      storage: 5Gi
