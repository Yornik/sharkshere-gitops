apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: muse-data-ssd
  namespace: muse
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: truenas-ssd
  resources:
    requests:
      storage: 2Gi
